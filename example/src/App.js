import React, { Component } from 'react';

// import GridChecker from 'react-grid-checker';

export default class App extends Component {
  state = {
    loading: false,
  };

  render() {
    return <div />;
  }
}
